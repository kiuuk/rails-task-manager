<h1>New Task</h1>

<!-- <form action="/add" method="POST">
   <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <div>
    <label for="add-title">Title</label>
    <input type="text" name="task[title]" id="add-title">
  </div>
  <div>
    <label for="add-detail">Details</label>
    <input type="text" name="task[details]" id="add-detail">
  </div>
  <input type="submit" value="submit">
</form> -->


<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_field :details %>

  <%= f.label :completed %>
  <%= f.check_box :completed %>

  <%= f.submit %>
<% end %>
